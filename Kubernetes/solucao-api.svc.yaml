apiVersion: v1
kind: Service
metadata:
  name: solucao-laser-back-svc
  namespace: solucao-laser
  labels:
    app: solucao-laser-back
spec:
  ports:
  - protocol: TCP
    port: 80
  selector:
    app: solucao-laser-back
